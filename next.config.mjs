module.exports = nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/jackson-portfolio",
  output: "export",
  reactStrictMode: true,
};

module.exports = nextConfig;
