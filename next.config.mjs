/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/jackson-portfolio",
  assetPrefix: "/jackson-portfolio",
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
